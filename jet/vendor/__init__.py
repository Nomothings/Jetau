"""Vendored code from the NanoJev project (unified game pipeline and the
decision-predictor inference runtime). Kept as close to upstream as possible;
see each module's docstring for provenance and local modifications."""
